# Same-origin (recommended) — Next.js proxies to the FastAPI backend.
NEXT_PUBLIC_API_BASE_URL=/api/v1
# Optional upstream override for the rewrite in next.config.js:
# LEADSENSE_API_ORIGIN=http://127.0.0.1:8000

